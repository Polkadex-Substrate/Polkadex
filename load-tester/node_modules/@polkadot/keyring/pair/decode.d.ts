import type { KeyringPair$JsonEncodingTypes } from '../types';
import type { PairInfo } from './types';
declare type DecodeResult = PairInfo & {
    secretKey: Uint8Array;
};
export declare function decodePair(passphrase?: string, encrypted?: Uint8Array | null, encType?: KeyringPair$JsonEncodingTypes[]): DecodeResult;
export {};
