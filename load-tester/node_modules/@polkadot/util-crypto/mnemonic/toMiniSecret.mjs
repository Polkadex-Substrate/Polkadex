// Copyright 2017-2021 @polkadot/util-crypto authors & contributors
// SPDX-License-Identifier: Apache-2.0
import { stringToU8a, u8aToBuffer } from '@polkadot/util';
import { bip39ToMiniSecret, isReady } from '@polkadot/wasm-crypto';
import { pbkdf2Encode } from "../pbkdf2/index.mjs";
import { mnemonicToEntropy } from "./toEntropy.mjs";
export function mnemonicToMiniSecret(mnemonic, password = '', onlyJs = false) {
  if (isReady() && !onlyJs) {
    return bip39ToMiniSecret(mnemonic, password);
  }

  const entropy = u8aToBuffer(mnemonicToEntropy(mnemonic));
  const salt = u8aToBuffer(stringToU8a(`mnemonic${password}`)); // return the first 32 bytes as the seed

  return pbkdf2Encode(entropy, salt).password.slice(0, 32);
}