import type { Codec, Constructor, InterfaceTypes, Registry } from '../../types';
export declare function typeToConstructor<T = Codec>(registry: Registry, type: keyof InterfaceTypes | Constructor<T>): Constructor<T>;
