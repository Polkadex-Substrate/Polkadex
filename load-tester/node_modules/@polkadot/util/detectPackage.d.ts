interface PackageJson {
    name: string;
    version: string;
}
declare type FnString = () => string | undefined;
/**
 * @name detectPackage
 * @summary Checks that a specific package is only imported once
 */
export declare function detectPackage({ name, version }: PackageJson, pathOrFn?: FnString | string | false): void;
export {};
