name: Build-CI
on:
  pull_request:
    branches: [frank.cicd]
  push:
    branches: [frank.cicd]
jobs:
  build:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v2
      - name: Cache Rust Dependecies
        uses: actions/cache@v2
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
      #       - name: Install toolchain
      #         run: ./install_rust.sh
      - name: Build Project
        # if: github.event_name == 'push'
        run: cargo build release
      - name: Set Node.js version 14.17.0
        uses: actions/setup-node@v1
        with:
          node-version: 14.17.0
      - name: Install packages
        run: npm install
      - name: List files in the repository
        run: |
          ls ./target
      - name: List files in the repository
        run: |
          ls ./target/release/wbuild/
      - name: Runtime Upgrade
        if: github.event_name == 'pull_request'
        run: node ./runtime_upgrade.js 0xe5be9a5092b81bca64be81d212e7f2f9eba183bb7a90954f7b76361f6edb5c0a wss://blockchain.polkadex.trade
      - run: cargo clean
      - name: Test Project
        run: cargo test
      - name: Run Cargo Trampulin
        if: github.event_name == 'push'
        uses: actions-rs/tarpaulin@v0.1
        with:
          version: '0.15.0'
          args: '--exclude polkadex-node node-polkadex-runtime polkadex-primitives  --workspace'
      - name: Upload to codecov.io
        if: github.event_name == 'push'
        uses: codecov/codecov-action@v1.0.2
        with:
          token: ${{secrets.CODECOV_TOKEN}}
      - name: Archive code coverage results
        if: github.event_name == 'push'
        uses: actions/upload-artifact@v1
        with:
          name: code-coverage-report
          path: cobertura.xml
