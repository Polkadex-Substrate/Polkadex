sp_api::decl_runtime_apis! {
    pub trait PolkadexIdoApi {
        //fn rounds_by_investor(account : T::AccountId) -> Vec<(T::Hash, FundingRound<T>)>;
        //fn rounds_by_creator(account : T::AccountId) -> Vec<(T::Hash, FundingRound<T>)> ;
    }
}
